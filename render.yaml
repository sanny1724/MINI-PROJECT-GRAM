services:
  # Complete Telangana GRAM Governance Fullstack Web Application
  - type: web
    name: gram-governance
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
      - key: DATABASE_URL
        value: sqlite:///./gram.db
      - key: SECRET_KEY
        generateValue: true
